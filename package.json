{
    "name": "futurestock-prediction-root",
    "version": "1.0.0",
    "description": "Root package for Railway deployment",
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd ../backend && python3 -m ensurepip && python3 -m pip install -r requirements.txt",
        "start": "cd backend && gunicorn --bind 0.0.0.0:$PORT app:app"
    }
}
