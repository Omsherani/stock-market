{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "/opt/venv/bin/gunicorn --bind 0.0.0.0:$PORT --pythonpath backend app:app"
    }
}