{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd backend && /opt/venv/bin/gunicorn --bind 0.0.0.0:$PORT app:app"
    }
}
